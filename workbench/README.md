# Workbench File

This folder contains the original MySQL Workbench SQL script used while building the portfolio project.

The script preserves the full practice/investigation workflow in one file. The cleaner, recruiter-facing queries are also separated by topic in the `queries/` folder.
